const initialState = {
    isFetching: false,
    admin:'',
    volunteer:'',
    student:'',
    error:''
}

export const reducer = (state = initialState, action ) => {
    switch(action.type){
        default:
            return state
    }
};